import React from "react";
import Layout from "../components/Layout";

export const metadata = {
  title: "About",
};

const page = () => {
  return (
    <>
      <Layout></Layout>
    </>
  );
};

export default page;
